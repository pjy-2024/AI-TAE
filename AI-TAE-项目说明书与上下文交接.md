# AI 智能测试辅助引擎（AI-TAE）· 项目总览与上下文交接文档

> 版本：v0.1 ｜ 日期：2026-09-03 ｜ 状态：方向已定，代码未开始
> 用途：① 给本人看的项目说明；② 作为“上下文交接”，可整篇粘贴给新聊天/助手继续推进。

---

## 0. 使用方法（给新对话）

把本文档整篇发给新对话后，第一句可以这样说：

> “请阅读这份项目文档，按其中【第 10 节·给新聊天的交接说明】继续。第一步先做任务 1：确认被测项目并细化 V1 技术方案。”

所有代码与文档统一写入桌面目录：
`C:\Users\彭井艺\Desktop\秋招项目\项目作品\04-AI智能测试辅助引擎-AITAE`

---

## 1. 背景与上下文摘要（当前聊天脉络）

- **用户**：苏州大学软件工程专业，2027 届毕业生（27 届），目前大四。
- **当前重心**：考研为主（时间紧、任务重）；求职为备选/保底，目标是 2027 春招与暑期实习。
- **求职方向**：软件测试 / 测试开发为主，同时希望可投后端研发（项目需两者兼顾）。
- **硬约束**：
  1. 不找、不依赖外部数据集；
  2. 时间盒管理：考研初试（约 2026 年 12 月）前只做轻量工作；初试后集中 2–3 周冲刺；
  3. **项目必须真实落地、使用真实数字，拒绝编造数据或“包装成已落地”**——面试官会追问、会点开仓库看 commit 与 issue。
- **协作分工**：AI/助手负责把编码工程量压到最小，并逐段讲解；本人负责理解每个设计决策、跑通验证、采集真实数据、准备面试讲解。
- **已做决策**：主项目定为 **AI-TAE**，采用“最小真实版”：**V1 完整 + V2 核心子集 + V3 最小原型（或写入 Roadmap）**。
- **备选方向（仅作 Roadmap 谈资，不投入开发）**：智能日志异常根因定位系统（需要日志+代码仓库数据，与“不找数据”约束冲突，暂缓）。

---

## 2. 项目一句话定义与电梯陈述

**定义**：一个把 LLM 深度嵌入测试工作流的“AI 测试辅助引擎”——输入接口文档自动生成可执行 pytest 用例；UI 用例因页面变化失败时，基于本地知识库（RAG）+ 本地 KV 缓存实现“脚本自愈”；并用评测手段区分真 Bug 与 Flaky，量化自身效果。

**电梯陈述（面试开场用；数字待实测后填入，未测前不要说）**：
> “我发现 UI 自动化测试最大的隐性成本是‘页面一改、脚本就废’的维护。我实现了一个 AI 测试辅助引擎：接口文档进来自动生成用例初稿；元素变化导致失败时，先查本地历史修复库，查不到再让大模型给出修复，并记录真实的自愈成功率与耗时；最后用 golden 标注评测 LLM 判定‘真 Bug / Flaky’的准确率。”

**为什么 HR/面试官会认可（一句话）**：方向踩中工业界真痛点（脚本自愈），AI 不是花架子而是嵌入真实工作流，且每一层都有可追问的工程细节。

---

## 3. 项目背景（为什么做）

### 3.1 行业痛点
- UI 自动化测试“脆弱”：选择器/页面结构一变，脚本批量失效，**维护成本**是 QA 团队真实痛点。
- AI 生成测试很热，但两大难题未解决：① 生成的用例常跑不起来、断言无效；② 无法证明它真能发现缺陷。
- 传统“断言数值”判定结果很死板，真 Bug 与环境波动（Flaky）难区分，浪费大量排查时间。

### 3.2 工业界与学界佐证（证明这是“真问题”而非自嗨）
- **Healenium**（Selenium 生态自愈库）：https://github.com/healenium/healenium —— 证明“脚本自愈”是工业刚需。
- **微软 TestExplora**：仓库级基准，研究 LLM 生成测试能否主动发现缺陷：https://github.com/microsoft/TestExplora
- **NeuroFlake**（EASE 2026）：用 LLM 做细粒度 Flaky Test 分类的研究工作（说明 V3 方向有学术价值）。

### 3.3 AI 时代的定位
- 不是“套壳 ChatBot”：AI 只嵌入 **生成 → 自愈 → 判定** 三个真实环节，核心价值在工程可靠性、成本与评测。
- 契合测试/测开岗；同时展示后端工程能力（异步、存储、可靠性），因此也能投后端研发岗。

---

## 4. 运用场景（谁会用它、什么时候用）

- **S1 接口用例初稿生成**：把 OpenAPI/Swagger 文档交给引擎 → 产出可执行的 pytest 用例初稿 → 人工审阅后入库。定位是“减少手写、加速”，不追求全自动无人值守。
- **S2 UI 失败自愈**：回归测试因元素选择器失效而失败 → 引擎抓取失败信息与当前页面结构 → **先查本地历史修复库（KV/RAG）→ 未命中再问 LLM** → 给出修复建议，默认人工确认后应用。
- **S3 失败定性**：区分“真 Bug / 环境波动（Flaky）/ 用例本身问题”。
- **S4 经验沉淀**：每次修复写入本地知识库，同类失败下次直接复用，并记录命中率。

**目标使用者**：先自己 + 1–3 名同学真实使用；被测对象选 1 个真实开源小 Web 项目。
**明确不做（防止范围膨胀）**：多租户 SaaS、企业级 CI 集成平台、UI 录制器、分布式部署。

---

## 5. 项目功能与版本规划

### V1（必做）· AI 生成接口测试用例
- 输入解析：支持 OpenAPI/Swagger（JSON/YAML）导入。
- 用例生成：LLM 生成 pytest 用例，**输出受 JSON Schema 约束**（结构化、可执行），而非裸 Markdown。
- 代码落盘：Parser 负责剥离/校验/写入用例文件；非法输出自动带错误信息重试（限次）。
- 执行：pytest 运行，收集通过/失败/错误堆栈。
- 质量门禁：统计 **用例可执行率、通过率**（第一个真实数字）。
- 可靠性：处理 **429 限流**——指数退避 + 抖动（jitter），并解释为什么不能盲目重试。

### V2 核心子集（必做）· 失败自愈 + 本地知识库
- 失败捕获：读取 pytest 失败信息与页面 DOM/结构。
- **自愈**：元素定位失败时，先查本地 KV 缓存（错误签名 → 修复策略）；未命中再让 LLM 分析新页面结构给出修复。
- 本地知识库 RAG：用 ChromaDB 存历史“错误签名 → 修复文档/案例”，检索辅助修复。
- 本地 KV：用 diskcache 做“错误签名 → 修复策略”缓存，降低 LLM 调用次数、延迟与成本；**真实统计缓存命中率**。
- 安全护栏：修复建议默认不自动应用（人工确认）；仅在测试环境执行。

### V3（最小原型，或写入 Roadmap）· 智能判定与评测
- LLM-as-Judge：根据失败日志/页面信息判定“真 Bug / Flaky / 用例问题”。
- golden 标注：SQLite 存 50–100 条人工标注样本，计算 judge 与人工标注的**一致率**（证明判定可信，而不是自说自话）。
- 双模型投票：作为 Roadmap（成本翻倍，先不做或最后做）。

### 明确不做（防止范围膨胀）
- 不做多租户/账号体系；不做 UI 自动录制；不做企业级 CI 插件；V3 不做双模型投票（除非时间富余）。

---

## 6. 技术栈（含取舍理由，面试要能讲“为什么”）

| 模块 | 选型 | 为什么 / 取舍 |
|---|---|---|
| 核心语言 | Python 3.10+ | 测试生态最全；本人熟悉 |
| AI 调用 | DeepSeek / Qwen（OpenAI 兼容接口） | 国内可访问、便宜；走统一接口 |
| 编排 | **手写 pipeline**（不依赖 LangChain） | 更小、可控、能讲清每一环；框架只增加黑盒 |
| 本地 KV | diskcache | 测试环境零依赖、开箱即用；**不叫“迷你 Redis”**——诚实说法是“本地嵌入式 KV”，Redis 是多实例/分布式的正解 |
| 向量库 | ChromaDB（嵌入式） | 数据量小（几万条以内），本地 CPU 足够，无需 ES/分布式向量库 |
| 持久化 | SQLite | golden 标注、历史记录，零运维 |
| 测试执行 | pytest + Playwright（UI）/ requests（API） | 生态成熟；Hook 机制（setup/teardown/fixture） |
| 沙箱 | Docker（执行 LLM 生成代码） | 安全隔离，防止恶意/错误代码影响宿主机 |

---

## 7. 底层问题与解决思路（面试深水区，逐条背熟）

> 每条都按“问题 → 方案 → 为什么/取舍 → 面试官可能追问”来准备。

1. **LLM 输出不可直接执行**
   方案：Schema 约束 + 本地校验 + 失败重试。
   追问：可执行率多少？重试上限怎么定？为什么不用裸 Markdown？

2. **429 限流**
   方案：指数退避 + 抖动；区分“可安全重试”与“不可重试”。
   追问：429 的 Retry-After 怎么用？并发场景怎么控速（令牌桶）？

3. **自愈会“误修”**（把真 Bug 当成页面变化修掉）
   方案：只对“元素定位失败”类错误自愈；修复默认人工确认；记录自愈前后的 diff。
   追问：如何防止自愈掩盖真 Bug？误修率怎么统计？

4. **KV 缓存命中率可能很低**
   方案：缓存 key 用“错误签名”（错误类型+元素特征+页面指纹），value 为修复策略；**先跑真实数据再谈命中率**，命中率低也能讲清原因。
   追问：命中率多少？为什么不直接全问 LLM？（答：成本与延迟，但要拿数据说话）

5. **RAG 检索质量**
   方案：小规模自建评测（修复建议是否有用）→ 人工抽检。
   追问：检索不到怎么办？向量库为什么不用 ES？（答：数据量小，本地够用，讲清取舍）

6. **Judge 判定是否可信**
   方案：golden 人工标注 + 一致率评测；不吹“双模型投票=正确”。
   追问：golden 哪来的？一致率多少？和人工比差多少？

7. **成本控制**
   方案：只在失败时调 LLM；命中缓存不调；用例生成用小模型初筛。
   追问：单次失败修复的平均成本？生成 100 条用例花多少钱？

8. **安全（执行 LLM 生成代码）**
   方案：Docker 沙箱、断网、只读被测代码、资源限制。
   追问：为什么必须沙箱？怎么防提示注入？

9. **可观测性**
   方案：结构化日志（每次修复/判定可追溯）、指标（成功率/耗时/成本）。
   追问：怎么定位一次“自愈失败”？

---

## 8. HR/面试官会着重注意的点与细节

### 含金量由什么决定（HR 视角）
1. **岗位匹配度**：本项目对口测试/测开，也能讲成后端工程。
2. **能讲多深**：能讲清“为什么这么设计、出过什么问题、做过什么取舍”的人，含金量远高于照搬教程。
3. **证据链**：真实使用者、真实数字、commit 历史、README 架构图与截图。

### 本项目的加分点（要在文档/简历/讲解中反复强化）
- 自愈是**工业刚需**（有 Healenium 等佐证，不是自嗨）；
- V1 的 Parser/重试体现**生产环境稳定性认知**；
- 本地 KV + RAG 体现**成本与延迟意识**（“先查缓存再问模型”）；
- Judge + golden 评测体现**“如何证明 AI 靠谱”的前沿意识**（对应微软 TestExplora / NeuroFlake 的方向）。

### 雷区（必须避免，否则减分甚至被判定造假）
- ❌ 编造数字（“2 小时→5 分钟”没测过就是造假）；
- ❌ 硬蹭术语（diskcache 叫“迷你 Redis”、几万条数据吹“手动 Top-K 粗排”）；
- ❌ 只讲框架名不讲取舍（LangChain 做了什么说不清）；
- ❌ 没有 commit 历史/README 的“落地”声明。

### “落地证据链”清单（做完一项勾一项，全部真实）
- [ ] 选定的 1 个真实开源小项目（被测对象）
- [ ] V1：可执行率 / 通过率（真实数字）
- [ ] V2：≥5 个真实 UI 改动 case + 自愈成功率/耗时
- [ ] V2：缓存命中率统计
- [ ] V3（若做）：judge 与 golden 的一致率
- [ ] 公网部署 URL（或可运行演示）
- [ ] README：架构图 + 截图 + 快速开始
- [ ] 演示视频（2–3 分钟）
- [ ] git commit 历史真实连续
- [ ] （可选）开源到 GitHub 并收集真实反馈（star/issue/使用者）

### 讲解原则（替代“背台词”）
面试讲项目的固定结构：**背景一句话 → 我遇到的真实问题 → 我的设计决策与取舍 → 真实数据 → 我踩过的坑与反思**。每个数字都来自上面的清单。

---

## 9. 面试 Q&A 题库（实现后逐个填真实答案，未填前标注【待填】）

**HR/主管面**
1. 为什么做测试方向？和考研如何平衡？
2. 这个项目和别人调 API 的 demo 有什么区别？
3. 你怎么证明项目是真实落地的？
4. 遇到最大的困难是什么？怎么解决的？

**技术一面（项目细节）**
5. LLM 生成的用例可执行率多少？怎么提高的？
6. 429 怎么处理？重试会不会重复计费？
7. 自愈的准确率/误修率？怎么防止把 Bug 当页面变化修掉？
8. KV 缓存命中率多少？为什么还要 RAG？
9. 为什么用 ChromaDB 不用 ES？数据量多大？
10. Judge 怎么证明可信？golden 数据怎么来的？
11. 成本怎么控制？生成 100 条用例花了多少钱？

**技术二面（底层/系统）**
12. 如果让你设计成多用户/上 CI，架构怎么改？
13. LLM 生成代码的安全风险？怎么隔离？
14. 自愈流程的完整时序？（失败→捕获→查缓存→RAG→LLM→确认→重跑）
15. 如果换一个模型，你的系统会崩吗？（答：Schema 约束 + 评测回归）

---

## 10. 给新聊天的交接说明与协作规则

**当前进度**：本文档 v0.1 已就绪；代码尚未开始。优先做“任务 1”，经本人确认后再写代码。

**任务优先级**：
1. 任务 1：确认被测开源小项目（建议一个简单 Web 应用，如图书管理/待办清单，能本地跑、含登录/表单/列表等可测元素）+ 细化 V1 技术方案（目录结构、接口设计、LLM 调用封装）。
2. 任务 2：搭 V1 骨架（OpenAPI 导入 → 生成 → Parser → pytest 执行 → 可执行率统计）。
3. 任务 3：V2 自愈 + ChromaDB + diskcache（在 V1 真实跑通后）。
4. 任务 4：V3 最小 judge 原型 + golden（时间富余再做）。
5. 贯穿：README、真实数字采集、面试 Q&A 答案填写。

**协作规则（必须遵守）**：
- 代码可大量代写，但**每个模块必须附带“给本人看的讲解”**：为什么这么写、面试怎么讲、可能被追问什么。
- **禁止编造任何数字/成果**；所有指标先用【待实测】占位，实现后填真实值。
- 主动提醒时间盒与范围控制：本人考研为主，任何“顺手多做”的需求都要先说明成本再决定。
- 写文件一律到桌面目录 `C:\Users\彭井艺\Desktop\秋招项目\项目作品\04-AI智能测试辅助引擎-AITAE`（若沙箱无权限需先申请）。
- 语言：中文沟通；代码注释与 README 用中文为主（本人可读性优先）。

---

## 11. 目录规划（建议）

```
04-AI智能测试辅助引擎-AITAE/
├── docs/            # 本文档、设计、面试问答
├── src/aiae/        # 主代码（parser、generator、healer、judge、kv、rag）
├── tests/           # 自身单元测试
├── samples/         # 被测项目/样例 OpenAPI
├── data/            # SQLite、ChromaDB、缓存（不入库）
├── scripts/         # 一键运行
├── README.md        # 架构图+截图+快速开始
└── requirements.txt
```

## 12. 里程碑（时间盒）

- **现在 → 考研初试（2026-12）**：每周极少量时间——只做任务 1（选型/选被测项目）与文档维护，不写大代码。
- **初试后第 1 周**：V1 完整跑通 + 真实可执行率/通过率。
- **初试后第 2 周**：V2 自愈 + KV/RAG + ≥5 个真实改动 case。
- **初试后第 3 周**：部署 + README + 演示视频 + Q&A 填答案；V3 视时间决定。
- **春招投递前**：证据链清单全部勾完。
