# AI-TAE · 阶段进度与交接（2026-09-03）

> 用途：跨会话交接。下一阶段在 Codex「项目」里新建对话时，把本文与 AI-TAE-项目说明书与上下文交接.md 一起带过去。
> 时间盒：考研初试（约 2026-12）前只做轻量工作；V1 大代码留到初试后集中做。

## 一、当前进度（真实记录，无编造）

1. 骨架 v0.1 已建并 push（github / gitee / gitcode 三远端）：目录结构、接口契约、metrics 口径、6 个冒烟测试通过。
2. C 盘独立环境已重建（D 盘只读保护后）：
   - Python 3.12.14：C:\Users\彭井艺\AppData\Local\Programs\Python\python312\python.exe（python-build-standalone 官方二进制，纯解压、零注册表、不依赖 D 盘）
   - AI-TAE 项目 venv：项目根 .venv（已 gitignore），依赖装好，editable 安装成功
   - 依赖清单：requirements.txt（运行时）+ requirements-dev.txt（+pytest），权威源 pyproject.toml
3. 被测项目已选定并跑通：manojnd9/todo_app，固定 commit f3cf7eeb...，细节见 samples/README.md；OpenAPI 已导出 samples/openapi/todo_app-openapi.json。
4. 【任务 2 已启动】parser/openapi.py 完成：读 OpenAPI/Swagger → Operation 归一化（OpenAPI 3.x + Swagger 2.0 桥接），真实样例 19 个 Operation / 17 path 解析通过；新增 parser 测试后全量 35 个通过。细节与契约细化见 docs/v1-technical-design.md §4 实现记录。
5. parser/codec.py 完成：LLM 文本剥围栏 → JSON 结构校验 → ast 静态校验 → 统一文件头落盘（错误信息精确到 tests[i] 供限次重试）；新增 21 个 codec 测试后全量 56 个通过。
6. llm/client.py 完成：OpenAI 兼容 chat/completions 封装——关掉 SDK 自带重试（max_retries=0）自实现 429/5xx 退避（Retry-After 优先 + 指数退避 + 抖动），usage/latency/retries 记账；15 个 mock 测试（不联网不烧钱），全量 71 个通过。注意：依赖 openai>=1.30 实际装到 3.7.0（vendored httpx2），API 兼容可用；锁已测版本留待初试后。
7. generator 完成：编排 Operation → Prompt → LLM → codec 校验 →（不合格带精确错误回传，语义层限次 ≤2 次改写）→ 逐条落盘 + GenerationReport；批处理单点失败不中断。9 个 mock 测试（fake LLM 按剧本表演），全量 80 个通过。
8. runner 完成：子进程（.venv python -m pytest）执行生成用例 + junitxml 收集（passed/failed/errors/skipped/耗时）；自动确保 conftest 提供 base_url fixture；踩到并解决两个 pytest 坑（--rootdir 防无权限目录扫描、--continue-on-collection-errors 防一个坏文件中断整批）。8 个测试（含真实子进程集成），全量 88 个通过。
9. cli 完成：`aiae generate / run / selfcheck` 串起 V1 全流程（generate 前置校验 API Key；run 报数先报口径：generated≈pytest 收集数、可执行=passed+failed）；缺省路径全部指向 data/ 产物约定。7 个测试，全量 95 个通过。至此 V1「机械部分」（parser/codec/llm/generator/runner/cli）全部完成，剩端到端真实验证。
10. 端到端首轮真实跑通（DeepSeek deepseek-chat，被测服务 :8010）：19 接口真实生成 19 条草稿全部落盘；真实 run 得首轮基线（见文末「端到端首轮基线」小节）。过程中修复两个真实问题：① config.py 从未加载 .env（补 load_dotenv）；② 不同接口被 LLM 起同名 test 函数导致落盘互相覆盖丢数据（改为 operation_id+test.name 唯一命名）。

## 二、关键教训（面试 / 工程都值得讲）

1. 沙箱限制：Codex 沙箱账户不能写注册表(HKCU)，标准安装器装不了 → 用官方独立二进制纯解压。体现「环境可复现、与机器解耦」的工程意识。
2. 依赖漂移（最重要）：被测项目按 2024 底依赖写（fastapi 0.115.6 / starlette 0.41），装最新 starlette 1.6 后所有 Jinja 页面 500（TemplateResponse 签名变了）。对策：被测项目按其锁定版本跑，不追最新——这正是「固定 commit + 锁版本」的意义。
3. 固定被测版本 = 记录不可变 commit，而不是「最新代码」（最新会自己动，没法评测与复现）。
4. poetry 锁 python=3.11.0，且 requirements 带 python_full_version==3.11.0 标记 → 在 3.12 上不能直接 -r requirements.txt，按包名装兼容版本即可（已在 3.12.14 跑通）。
5. 自己的依赖也会漂移：AI-TAE 的 openai>=1.30 无上限，pip 装到了 3.x（3.7.0，vendored httpx2）。好在 chat/completions 与异常层次向后兼容、可用；启示：自己的依赖同样要锁「已测版本」（待办，初试后处理）。

## 三、下一步（任务 2：V1 代码，初试后集中做）

按 docs/v1-technical-design.md 第 4 节顺序填实现：

1. ✅ parser/openapi.py（2026-09-03 完成）：读 OpenAPI/Swagger → Operation 列表（3.x 归一化 + 2.0 桥接）
2. ✅ parser/codec.py（2026-09-03 完成）：LLM 输出 JSON 校验 + ast 静态校验 + 落盘
3. ✅ llm/client.py（2026-09-03 完成）：OpenAI 兼容调用 + 429 退避 + 记账
4. ✅ generator（2026-09-03 完成）：Operation → Prompt → LLM → codec 校验重试 → 落盘 + GenerationReport
5. ✅ runner（2026-09-03 完成）：子进程 pytest + junitxml 收集，自动确保 conftest(base_url)
6. ✅ cli（2026-09-03 完成）：aiae generate / run / selfcheck 串起 V1 全流程 + metrics 口径对接
7. 端到端验证：配 LLM key + 启动被测服务，填第一批【待实测】数字（← 下一步，见 samples/README.md）
8. 质量门禁：可执行率 / 通过率【待实测】

## 四、V2 规划（任务 3）

- 在快照 data/targets/todo_app 里做「真实 UI 改动」（改 Jinja 模板的 id/class/结构）制造失败
- 自愈链路：KV(diskcache) → RAG(ChromaDB) → LLM → 人工确认，统计缓存命中率 / 自愈成功率【待实测】

## 五、命令速查

- AI-TAE 自检与测试（先激活项目 .venv）：
  - aiae selfcheck
  - python -m pytest -q
- 被测项目启动（在 data/targets/todo_app 目录）：
  - .venv\Scripts\python.exe -m uvicorn todo_app.main:app --port 8010
- 被测项目 venv 创建：
  - C:\Users\彭井艺\AppData\Local\Programs\Python\python312\python.exe -m venv data\targets\todo_app\.venv
- 依赖安装镜像：https://pypi.tuna.tsinghua.edu.cn/simple




## 六、V1 端到端真实结果（2026-09-03，三轮迭代至定稿，全部真实）

条件：DeepSeek deepseek-chat（key 走 .env）；被测 todo_app 固定 commit 起在 :8010；19 个接口一次性真实生成。

| 轮次 | 生成 | 执行（passed/failed/errors） | 可执行率 | 通过率 | 说明 |
|---|---|---|---|---|---|
| ① 无鉴权 | 19/19 落盘 | 8/11/0 | 100% | 42.1% | 需认证接口全 401 + 登录假账号：生成策略未处理鉴权 |
| ② auth_headers | 19/19 | 13/6/0 | 100% | 68.4% | admin 接口 401（普通 user 无 admin 角色）、登录被「改密码」用例污染、资源 id 写死 404/405 |
| ③ 定稿 | 19/19 | 19/0/0 | 100% | 100% | fresh_user + role=admin + created_todo_id；连跑两次均 19/19 可复现 |

- 失败分类（①②轮，人工）：全部为「生成用例的准备不足」（鉴权/角色/资源 id），**无一为被测项目真 Bug**
- 三轮对应的工程改进（可面试讲）：① codec/ast 门禁保证可执行率 100%（LLM 输出都能跑）；② conftest 把登录态下沉为 session fixture + Prompt 感知 security；③ 共享 fixture 会被「改密码」类用例污染 → 登录用例改用 function 级 fresh_user；被测项目 admin 接口要求 role=admin 而普通接口只查登录 → registered_user 注册 role=admin 通吃；todo_app 创建接口返回 201+null 拿不到 id → created_todo_id 创建后从列表按 seed 标题取回
- 定稿指标口径（见 v1-technical-design §7）：可执行率 = 19/19 = 100%；通过率 = 19/19 = 100%（分母=可执行）

真实数字纪律：以上全部为 2026-09-03 实际运行结果（junit 在 data/runs/latest.xml，gitignore 不入库），可复现；生成草稿在 data/generated_tests。
