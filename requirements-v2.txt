# V2 运行时依赖（UI 失败自愈）。安装：pip install -r requirements-v2.txt
# 实测版本（2026-09-04，.venv 已装）：diskcache 5.6.3 / chromadb 1.5.9 / playwright 1.62.0
# UI 驱动用系统 Edge（channel=msedge），无需 playwright install 下载浏览器。

diskcache==5.6.3
chromadb==1.5.9
playwright==1.62.0
